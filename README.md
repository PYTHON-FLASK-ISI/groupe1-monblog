# Mon Blog — Groupe 1

Projet Flask ISI : blog d'articles et catalogue de produits.

## Installation

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python run.py
```

L'application est accessible sur http://localhost:5000.
